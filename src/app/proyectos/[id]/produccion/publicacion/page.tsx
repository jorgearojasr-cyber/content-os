import { Card, SectionTitle } from "@/components/ui";

export default function PublicacionPage() {
  return (
    <Card>
      <SectionTitle subtitle="Seguimiento de la publicación de las piezas terminadas — todavía no implementado.">
        Publicación
      </SectionTitle>
      <p className="text-[13px] text-text-muted">Por ahora, usa el Calendario para planificar fechas de publicación.</p>
    </Card>
  );
}

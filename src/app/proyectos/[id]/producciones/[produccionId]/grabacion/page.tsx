import { Card, SectionTitle } from "@/components/ui";

export default function GrabacionPage() {
  return (
    <Card>
      <SectionTitle subtitle="Seguimiento del rodaje de las escenas planificadas — todavía no implementado.">
        Grabación
      </SectionTitle>
      <p className="text-[13px] text-text-muted">Por ahora, marca cada escena como &ldquo;Grabada&rdquo; desde su tarjeta en Escenas.</p>
    </Card>
  );
}
